# Design — AGT-06

Redact em `dispatch` antes de `into_blocks` / observer / `record`. Helper
`tool/redact.rs`. Regex é auxílio, o instrumento é a fixture fail-closed.

## Aprovação (SDD-04)

Pendente LGTM humano.
